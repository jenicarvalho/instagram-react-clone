import styled from "styled-components";

export const Container = styled.div`
  margin-top: 20px;
  background: #fff;
  border-radius: 3px;
  border: 1px solid #e6e6e6;
  padding: 16px;

  h2 {
    margin: 0;
    font-size: 14px;
    line-height: 18px;
    color: var(--primary);
  }
`;
